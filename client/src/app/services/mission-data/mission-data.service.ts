import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { IMissionData } from './interfaces/IMissionData.interface';
import { TerrainType } from './enums/TerrainType.enum';

@Injectable({
  providedIn: 'root',
})
export class MissionDataService {
  constructor() {}
}
